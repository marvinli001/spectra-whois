Use case: precise-object-edit
Asset type: revised high-fidelity desktop web application composition reference for SpectraWHOIS
Input image: Image 1 is the approved Luma "Command Workbench" UI comp. Preserve its shadcn/ui Luma visual language, neutral light palette, blue theme, Geist typography, Hugeicons character, header, spacing quality, border treatment, and default corner radius.
Primary request: revise only the idle/default state composition to match the user's feedback. The default screen must be much simpler and contain only the global header, a dominant domain search experience, and recent search history when history exists. Remove the entire "Lookup coverage" panel. Remove the result-preview panel and all rich information panels from the default state. Do not show protocol capability cards before a search.
Structure: full 1440x1000 desktop app screenshot, no browser chrome. Keep the slim 68px header. Under it, use an elegant asymmetric 8/4 layout with generous but controlled whitespace: left side has a focused search workbench with title "Know who is behind a domain.", visible label "Domain name", input "example.com", and primary action "Look up domain"; quiet example-domain actions may sit beneath the field. Right side shows a single "Recent lookups" panel only because example history is present, with 4 compact rows. The lower half remains calm empty page ground rather than feature cards or result previews. The search surface should visually suggest that it can smoothly compact into a top search strip after submission, but keep the still screen truthful.
Motion direction to imply in the static comp: controls have refined pressed/hover affordances, a soft blue focus ring, and spatial continuity. Do not add motion trails or decorative animation artifacts.
Text (verbatim where visible): "SpectraWHOIS", "Know who is behind a domain.", "Domain name", "example.com", "Look up domain", "Try an example", "iana.org", "icann.org", "cloudflare.com", "Recent lookups".
Constraints: preserve everything not named for change; one clear primary action; no coverage panel, no protocol cards, no results before search, no feature cards, no marketing claims, no fake metrics, no export, no customers, no people, no maps, no purple, no gradients, no glow, no glassmorphism, no em dash, no fake browser frame. This is an Operate tool, not a landing page.
